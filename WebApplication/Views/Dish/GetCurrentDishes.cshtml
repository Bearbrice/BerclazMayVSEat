@model IEnumerable<DTO.Dish>

@{
    ViewData["Title"] = "GetCurrentDishes";
}

<h2>GetCurrentDishes</h2>

@{
    decimal totalPrice = new decimal(0.0);
    int index = -1;
}

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.idDish)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.fk_idRestaurant)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            totalPrice += item.price;
            index++;
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.idDish)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.price)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.fk_idRestaurant)
                </td>
                <td>
                    @*@Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |*@
                    @*@Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |*@
                    @Html.ActionLink("Remove", "Delete", new { index })

                    @*@Html.ActionLink("Show delicious dishes", "GetDishes", "Dish", new { id = item.idRestaurant })*@


                </td>
            </tr>
        }
    </tbody>
</table>

<TotalPrice> Total price : @totalPrice <devise> CHF</devise> </TotalPrice>
