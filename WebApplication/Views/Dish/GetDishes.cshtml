@model IEnumerable<DTO.Dish>

@{
    ViewData["Title"] = "GetDishes";
}

<h2>Dishes</h2>

<table class="table">
    <thead>
        <tr style="background-color:darkcyan; color:white">
            <th>
                N°
            </th>
            <th>
                Dish
            </th>
            <th>
                Price
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.idDish)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.price)
                </td>
                <td>
                    @Html.ActionLink("Add to basket", "Edit", new { id = item.idDish })
                </td>
            </tr>
        }
    </tbody>
</table>

<a href="javascript:history.back()">Go back to restaurants</a>



